#!/bin/sh

harness="${top_srcdir}/tests/query-harness.sh"

export HOME=/home/test
export XDG_STATE_HOME="/path/with a few spaces"

arguments='state home'
expected='/path/with a few spaces'

. "$harness"
